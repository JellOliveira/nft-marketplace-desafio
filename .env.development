# Carregado automaticamente pelo Vite em `npm run dev` / `npm run build -- --mode development`.
# Aponta o cliente Socket.IO para o servidor de tempo real rodando localmente
# (ver realtime-server/README.md). Sem segredos aqui — é só um endereço local.
VITE_SOCKET_URL=http://localhost:4001
